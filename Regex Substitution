import re

def change(match):
    if match.group(0) == "&&":
        return "and"
    else:
        return "or"

n = int(input())

for _ in range(n):
    line = input()
    print(re.sub(r"(?<= )(&&|\|\|)(?= )", change, line))
