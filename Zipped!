n, x = map(int, input().split())
mark_sheet = []
for _ in range(x):
    mark_sheet.append(map(float, input().split()))
for student_marks in zip(*mark_sheet):
    average = sum(student_marks) / len(student_marks)
    print(f"{average:.1f}")
