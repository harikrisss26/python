thickness = int(input()) #This must be an odd number
c = 'H'

#Top 
for i in range(thickness):
    # This line prints leading spaces, 'H's, a center 'H', and trailing 'H's
    print((c*i).rjust(thickness-1)+c+(c*i).ljust(thickness-1))

#Top Pillars
for i in range(thickness+1):
    # This line prints two vertical pillars separated by spaces
    print((c*thickness).center(thickness*2)+(c*thickness).center(thickness*6))

#Middle 
for i in range((thickness+1)//2):
    # This line prints the full width horizontal section
    print((c*thickness*5).center(thickness*6))

#Bottom 
for i in range(thickness+1):
    # This line prints two vertical pillars separated by spaces
    print((c*thickness).center(thickness*2)+(c*thickness).center(thickness*6))

#Bottom Cone
for i in range(thickness):
    # This line prints trailing spaces, 'H's, a center 'H', and leading 'H's, inverted from the top cone
    print(((c*(thickness-i-1)).rjust(thickness)+c+(c*(thickness-i-1)).ljust(thickness)).rjust(thickness*6))
